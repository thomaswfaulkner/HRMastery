import React from 'react';
import ReactDOM from 'react-dom/client';
import IntegratedSuite from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <IntegratedSuite />
  </React.StrictMode>
);
